# The sponsor button GitHub renders on a repository page.
#
# The repository is public, so this button is rendered and reaches anyone who lands on
# the repository page. Most readers of the standard will not: they arrive at the
# documentation site, which does not read this file. The link that reaches them is the
# one in README.md, which the site publishes. Both are wanted; neither replaces the other.
#
# An earlier revision of this comment said the repository was private and the file was
# nearly inert. True when written, false the moment it was published.

github: HetCreep
